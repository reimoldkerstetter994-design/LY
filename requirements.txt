# Runtime uses the Python standard library plus requests (already common).
# beautifulsoup4 is optional: parsers fall back to html.parser if it is absent.
requests>=2.31.0
beautifulsoup4>=4.12.0

# Dev / tests
pytest>=8.0.0
