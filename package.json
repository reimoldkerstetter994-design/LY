{
  "name": "bytehaven",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bytehaven — a playable 3D open world built with Blender + Three.js",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5173",
    "assets": "bash scripts/generate-assets.sh",
    "test": "node --test tests/world.test.js"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.3"
  }
}
