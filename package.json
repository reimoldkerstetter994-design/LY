{
  "name": "devforge-world",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "DevForge 源码港 — a playable 3D developer-campus game authored in Blender and run in Three.js",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "blender:export": "bash scripts/export-blender.sh"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.3"
  }
}
